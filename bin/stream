#!/usr/bin/perl
package bin::stream;

use strict;
use warnings;

# ABSTRACT: command line tool to manage streams

=head1 SYNOPSIS

Available commands:

  commands: list the application's commands
      help: help command

      head: print several items from given input stream
      info: print info about given stream
       lag: show lag of given input stream
      list: List all streams
      pump: pump several items using given pumper

=cut

use StreamApp;
StreamApp->run;
